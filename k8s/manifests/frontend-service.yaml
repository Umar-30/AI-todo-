apiVersion: v1
kind: Service
metadata:
  name: todo-frontend-svc
  labels:
    app: todo-frontend
    app.kubernetes.io/name: todo-chatbot
    app.kubernetes.io/component: frontend
spec:
  type: NodePort
  selector:
    app: todo-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
      protocol: TCP
